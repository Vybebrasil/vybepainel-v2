import { bloqueou } from '../vybe_acesso.js';
import { getUsageDashboard, updateUsageSettings } from '../jarvis_usage.js';
export default async function handler(req, res) { res.setHeader('Access-Control-Allow-Origin', 'null'); res.setHeader('Access-Control-Allow-Methods', 'GET, POST, OPTIONS'); res.setHeader('Access-Control-Allow-Headers', 'Content-Type'); if (req.method === 'OPTIONS') return res.status(200).end(); if (bloqueou(req, res)) return; try { if (req.method === 'GET') { const days = req.query?.days; return res.status(200).json(await getUsageDashboard(days)); } if (req.method === 'POST') { const body = req.body || {}; const settings = await updateUsageSettings({ brl_per_usd: body.brl_per_usd, monthly_budget_brl: body.monthly_budget_brl, gemini_billing: body.gemini_billing }); const dashboard = await getUsageDashboard(body.days || 30); return res.status(200).json({ settings, dashboard }); } return res.status(405).json({ error: 'Method not allowed' }); } catch (error) { console.error('AI usage API failed:', error.message); return res.status(500).json({ error: 'Não foi possível carregar o monitoramento de IA.' }); } }
